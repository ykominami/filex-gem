# frozen_string_literal: true

RSpec.describe Filex do
  it 'has a version number' do
    expect(Filex::VERSION).not_to be_nil
  end

  it 'does something useful' do
    ret = true
    expect(ret).to be(true)
  end
end
